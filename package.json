{
  "name": "meteor-generate",
  "version": "0.12.0",
  "description": "Generate Meteor scaffolding on demand",
  "keywords": [ "meteor", "scaffold"],
  "author": "Adam Brodzinski",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AdamBrodzinski/meteor-generate.git"
  },

  "bin": { "mgen": "./bin/mgen" },

  "dependencies": {
    "commander": "*",
    "change-case": "*",
    "fs-extra": "0.13.0",
    "pluralize": "*"
  },
  "devDependencies": {
    "mocha": "*",
    "chai": "*",
    "coffee-script": "*"
  },
  "scripts" : {
    "test" : "make test"
  }
}
